interface:
  display_name: "Podman"
  short_description: "Repo-specific patterns and workflows for podman"
  default_prompt: "Use the podman repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true